# UX Polish (Variant 4) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Ship the four audit Variant-4 UX items as four serial micro-PRs: unified «Встречи» terminology (+ Russian pluralization), non-blocking folder stats in Settings, a dedup on/off checkbox, and an honest per-model cost forecast with forecast-vs-actual display.

**Architecture:** No new modules except a 15-line `plural_ru` helper in `utils.py` and one new `build_dedup_section` free function in the existing `settings_builder.py`. Everything else edits existing files following their established patterns (settings_builder section contract, pricing.py pure-leaf, window-sliced source tests).

**Tech Stack:** Python 3.10+, CustomTkinter, pytest, ruff. Spec: `docs/superpowers/specs/2026-06-11-ux-polish-design.md`.

**Dev-env notes (this machine):**
- Always run Python as `py -3` (`python` resolves to a foreign venv without pytest/ruff).
- Gate before every commit: `py -3 -m pytest` green + `py -3 -m ruff check . --exclude .cache --exclude .venv-build` clean.
- UI modules (`ui/*`, `audio_cutter`) must NOT be imported by tests — Linux CI has no PortAudio. All UI assertions are source-text checks (read file, slice by `def` markers).
- PR bodies / commit messages with non-ASCII or `"` go through files (`git commit -F`, `gh pr create --body-file`) — PowerShell 5.1 mangles inline args.
- PRs are strictly serial: branch PR-N+1 off main only after PR-N is squash-merged. PR-1's branch is `refactor/meeting-terminology`, created FROM `docs/ux-polish-spec` so the spec + this plan ride into main with it.

---

## PR-1 — terminology «Встречи» + plural_ru

Branch: `refactor/meeting-terminology` (from `docs/ux-polish-spec`).
PR title: `refactor(ui): unify meeting terminology to «Встречи»`.

### Task 1: `plural_ru` helper in utils.py

**Files:**
- Create: `tests/test_plural_ru.py`
- Modify: `utils.py` (append the function near the other small pure helpers)

- [ ] **Step 1: Write the failing test**

Create `tests/test_plural_ru.py`:

```python
"""Russian plural word-form helper (spec 2026-06-11, PR-1)."""
import pytest

from utils import plural_ru


@pytest.mark.parametrize("n,expected", [
    (0, "встреч"),
    (1, "встреча"),
    (2, "встречи"),
    (4, "встречи"),
    (5, "встреч"),
    (10, "встреч"),
    (11, "встреч"),     # 11–14 are always the many-form…
    (12, "встреч"),
    (14, "встреч"),
    (21, "встреча"),    # …but 21/121 go back to the one-form
    (22, "встречи"),
    (25, "встреч"),
    (100, "встреч"),
    (101, "встреча"),
    (111, "встреч"),    # 111 is the 11-exception at the next hundred
    (121, "встреча"),
])
def test_plural_ru_meetings(n, expected):
    assert plural_ru(n, "встреча", "встречи", "встреч") == expected
```

- [ ] **Step 2: Run test to verify it fails**

Run: `py -3 -m pytest tests/test_plural_ru.py -v`
Expected: FAIL — `ImportError: cannot import name 'plural_ru' from 'utils'`

- [ ] **Step 3: Write minimal implementation**

Append to `utils.py` (top level, after the existing small helpers):

```python
def plural_ru(n: int, one: str, few: str, many: str) -> str:
    """Russian plural word form for ``n``: 1 встреча / 2 встречи / 5 встреч.

    Returns the WORD ONLY — callers compose ``f"{n} {plural_ru(...)}"``.
    Handles the 11–14 exception at any hundred (11 встреч, 111 встреч,
    but 21 встреча, 121 встреча).
    """
    n = abs(n) % 100
    if 11 <= n <= 14:
        return many
    d = n % 10
    if d == 1:
        return one
    if 2 <= d <= 4:
        return few
    return many
```

- [ ] **Step 4: Run test to verify it passes**

Run: `py -3 -m pytest tests/test_plural_ru.py -v`
Expected: 17 PASS

- [ ] **Step 5: Commit**

```bash
git add tests/test_plural_ru.py utils.py
git commit -m "feat(utils): plural_ru helper for Russian counter labels"
```

### Task 2: meetings dialog + main-window button strings

**Files:**
- Modify: `ui/dialogs/meetings.py:1-7` (docstring), `:167`, `:186`, `:258`, `:261`
- Modify: `ui/app/builder.py:357`
- Modify: `tests/test_meetings_dialog_rename.py:35-70` (flip locks) + append grep-guard

- [ ] **Step 1: Flip the lock tests (failing first)**

In `tests/test_meetings_dialog_rename.py` replace the three dialog-string tests and the builder test (lines 35–70) with:

```python
def test_meetings_dialog_title_is_meetings():
    """Window title must be «Встречи» (terminology unified 2026-06-11)."""
    src = (REPO / "ui" / "dialogs" / "meetings.py").read_text(encoding="utf-8")
    assert "История транскрипций" not in src, (
        "Old window title must be gone"
    )
    assert '"Встречи"' in src or "'Встречи'" in src, (
        "Window title must be «Встречи»"
    )


def test_meetings_footer_label_renamed():
    """«Записей: N» → «Встреч: N» (fixed genitive — declension-safe)."""
    src = (REPO / "ui" / "dialogs" / "meetings.py").read_text(encoding="utf-8")
    assert "Записей:" not in src, "Old «Записей:» label must be gone"
    assert "Встреч:" in src, "New «Встреч:» label required"


def test_meetings_empty_state_renamed():
    """«Нет транскрипций» → «Нет встреч»."""
    src = (REPO / "ui" / "dialogs" / "meetings.py").read_text(encoding="utf-8")
    assert "Нет транскрипций" not in src
    assert "Нет встреч" in src


def test_builder_uses_meetings_button_text():
    """Main-window button text is «Встречи»."""
    builder = (REPO / "ui" / "app" / "builder.py").read_text(encoding="utf-8")
    assert '"Встречи"' in builder or "'Встречи'" in builder, (
        "Main window button must read «Встречи»"
    )
    # The OLD «История» text must be gone (the underscore name
    # _btn_history is a stable Python identifier — only label text changes).
    assert 'text="История"' not in builder, (
        "Old «История» button text must be gone from builder.py"
    )
```

And append at the end of the file:

```python
def test_no_miting_terminology_left_in_ui():
    """Terminology unified to «встречи» (2026-06-11) — no «митинг» strings
    remain anywhere under ui/ (titles, labels, docstrings)."""
    import re

    offenders = []
    for path in sorted((REPO / "ui").rglob("*.py")):
        src = path.read_text(encoding="utf-8")
        if re.search(r"[Мм]итинг", src):
            offenders.append(str(path.relative_to(REPO)))
    assert not offenders, f"«митинг» terminology found in: {offenders}"
```

- [ ] **Step 2: Run to verify the flipped locks fail**

Run: `py -3 -m pytest tests/test_meetings_dialog_rename.py -v`
Expected: 5 FAIL (title, footer, empty-state, builder button, grep-guard), 3 PASS

- [ ] **Step 3: Rename the strings**

`ui/dialogs/meetings.py` — replace the module docstring (lines 1–7):

```python
"""Meetings browser + read-only transcript viewer.

Renamed from history.py on 2026-05-28; UI terminology unified to
«Встречи» on 2026-06-11 (was «Митинги»). Files on disk are unchanged;
the underlying utils.list_history_entries / delete_history_entry
helpers keep their internal names (renames were UI-only).
"""
```

Line 167: `self.title("Митинги")` → `self.title("Встречи")`
Line 186: `header, text="Митинги",` → `header, text="Встречи",`
Line 258: `self._lbl_count.configure(text=f"Митингов: {len(entries)}{suffix}")` → `self._lbl_count.configure(text=f"Встреч: {len(entries)}{suffix}")`
Line 261: `msg = "Ничего не найдено" if query else "Нет митингов"` → `msg = "Ничего не найдено" if query else "Нет встреч"`

`ui/app/builder.py` line 357: `btn_frame, text="Митинги", command=app._open_meetings_dialog,` → `btn_frame, text="Встречи", command=app._open_meetings_dialog,`

- [ ] **Step 4: Run the file — grep-guard still red (settings/migration pending)**

Run: `py -3 -m pytest tests/test_meetings_dialog_rename.py -v`
Expected: only `test_no_miting_terminology_left_in_ui` FAILS (offenders: settings.py, settings_builder.py, migration.py); the other 7 PASS. Tasks 3–4 turn it green — commit comes after Task 4.

### Task 3: Settings strings (settings.py + settings_builder.py)

**Files:**
- Modify: `ui/dialogs/settings_builder.py:135`
- Modify: `ui/dialogs/settings.py:47`, `:300-309`, `:314`
- Modify: `tests/test_settings_dialog_meetings_section.py:1,14-19`

- [ ] **Step 1: Flip the section lock test**

In `tests/test_settings_dialog_meetings_section.py` replace line 1 and the first test:

```python
"""Source-text checks for the Встречи section in Settings."""
```

```python
def test_settings_has_meetings_section_card():
    """A section card titled «Встречи» exists in settings_builder.py."""
    src = BUILDER_PATH.read_text(encoding="utf-8")
    assert '"Встречи"' in src or "'Встречи'" in src, (
        "Settings builder must declare a section card with title «Встречи»"
    )
    assert "section_card" in src
```

- [ ] **Step 2: Run to verify it fails**

Run: `py -3 -m pytest tests/test_settings_dialog_meetings_section.py -v`
Expected: `test_settings_has_meetings_section_card` FAIL, other 3 PASS

- [ ] **Step 3: Rename the strings**

`ui/dialogs/settings_builder.py` line 135:
`section = section_card(dialog, parent, "Митинги", row=4)` → `section = section_card(dialog, parent, "Встречи", row=4)`

`ui/dialogs/settings.py` line 47:
`from utils import get_meetings_dir, save_config` → `from utils import get_meetings_dir, plural_ru, save_config`

`ui/dialogs/settings.py` lines 300–309 — replace the method with:

```python
    def _refresh_meetings_stats(self) -> None:
        """Compute «В этой папке: N встреч • X GB» and update the label."""
        from meetings_migration import count_meetings
        from ui.dialogs.migration import _fmt_size, _folder_size_bytes
        path = self._meetings_path_var.get()
        n = count_meetings(path)
        size = _folder_size_bytes(path)
        word = plural_ru(n, "встреча", "встречи", "встреч")
        self._meetings_stats_label.configure(
            text=f"В этой папке: {n} {word} • {_fmt_size(size)}",
        )
```

(Still synchronous — PR-2 makes it async; this PR only touches strings.)

`ui/dialogs/settings.py` line 314:
`title="Папка для хранения митингов",` → `title="Папка для хранения встреч",`

- [ ] **Step 4: Run to verify it passes**

Run: `py -3 -m pytest tests/test_settings_dialog_meetings_section.py tests/test_widget_tree_split.py -v`
Expected: all PASS (widget-tree locks unaffected — no `def _build_` added)

### Task 4: migration dialog strings

**Files:**
- Modify: `ui/dialogs/migration.py` imports + `:84`, `:97-100`, `:117-120`, `:215`, `:279`

- [ ] **Step 1: Rename the strings**

Add the import after the `theme` import block (after line 35):

```python
from utils import plural_ru
```

Line 84: `self.title("Перенос митингов")` → `self.title("Перенос встреч")`

Lines 97–100 — replace:

```python
        title_text = (
            "Перенос встреч" if mode == "first_launch"
            else "Перенести существующие встречи?"
        )
```

Lines 117–120 — replace:

```python
        word = plural_ru(n_src, "встреча", "встречи", "встреч")
        if mode == "first_launch":
            label1 = f"Найдено {n_src} {word} в старой папке:"
        else:
            label1 = f"В текущей папке {n_src} {word}:"
```

Line 215: `self.title("Перенос митингов")` → `self.title("Перенос встреч")`
Line 279: `text_main = f"Переношу митинг {done} / {total}:"` → `text_main = f"Переношу встречу {done} / {total}:"`

- [ ] **Step 2: Run the grep-guard — now green**

Run: `py -3 -m pytest tests/test_meetings_dialog_rename.py -v`
Expected: all 8 PASS (no «митинг» left under ui/)

- [ ] **Step 3: Commit**

```bash
git add ui/dialogs/meetings.py ui/app/builder.py ui/dialogs/settings.py ui/dialogs/settings_builder.py ui/dialogs/migration.py tests/test_meetings_dialog_rename.py tests/test_settings_dialog_meetings_section.py
git commit -m "refactor(ui): unify meeting terminology to «Встречи»"
```

### Task 5: PR-1 gate + push

- [ ] **Step 1: Full gate**

Run: `py -3 -m pytest` → green; `py -3 -m ruff check . --exclude .cache --exclude .venv-build` → clean.

- [ ] **Step 2: Push + PR**

```bash
git push -u origin refactor/meeting-terminology
gh pr create --head refactor/meeting-terminology --title "refactor(ui): unify meeting terminology to «Встречи»" --body-file <body.md>
```

PR body: Summary (terminology direction + plural_ru + spec/plan ride along) + Test plan checklist (pytest, ruff, flipped locks, grep-guard, user 60-s GUI smoke: main button, meetings dialog, Settings section title, folder picker title). **Wait for user merge before PR-2.**

---

## PR-2 — async folder-stats in Settings

Branch: `fix/settings-async-stats` (from main, after PR-1 merges).
PR title: `fix(settings): compute folder stats off the Tk thread`.

### Task 1: source-slice tests (red)

**Files:**
- Create: `tests/test_settings_async_stats.py`

- [ ] **Step 1: Write the failing tests**

```python
"""Folder stats must be computed off the Tk thread (spec 2026-06-11, PR-2).

Window-sliced source checks — settings.py imports CTk, which Linux CI
cannot import (no PortAudio via the ui package chain).
"""
from pathlib import Path

SETTINGS = Path("ui/dialogs/settings.py").read_text(encoding="utf-8")


def _stats_block() -> str:
    """Body of _refresh_meetings_stats (up to the next method def)."""
    start = SETTINGS.index("def _refresh_meetings_stats")
    end = SETTINGS.index("\n    def ", start + 1)
    return SETTINGS[start:end]


def test_stats_computed_in_worker_thread():
    block = _stats_block()
    assert "threading.Thread" in block, "size walk must run off the Tk thread"
    assert "daemon=True" in block


def test_stats_placeholder_shown_immediately():
    # The label must show feedback synchronously while the walk runs.
    assert "Подсчёт" in _stats_block()


def test_stats_guarded_by_generation_counter():
    # A stale walk result (user switched folders mid-scan) must be dropped.
    assert "_stats_gen" in _stats_block()


def test_stats_apply_guarded_against_dead_dialog():
    # self.after() from the worker raises TclError if the dialog is gone.
    assert "tk.TclError" in _stats_block()
```

- [ ] **Step 2: Run to verify they fail**

Run: `py -3 -m pytest tests/test_settings_async_stats.py -v`
Expected: 4 FAIL (method is still synchronous)

### Task 2: implement the async method

**Files:**
- Modify: `ui/dialogs/settings.py` — `_refresh_meetings_stats` only (`threading`, `tk`, `plural_ru` are already imported at module top)

- [ ] **Step 1: Replace the method**

```python
    def _refresh_meetings_stats(self) -> None:
        """Update «В этой папке: N встреч • X GB» without blocking Tk.

        The meetings folder may hold multi-GB recordings/ (since #93), so
        the os.walk size scan runs in a daemon thread. A generation
        counter drops stale results when the user switches folders while
        a previous scan is still walking.
        """
        from meetings_migration import count_meetings
        from ui.dialogs.migration import _fmt_size, _folder_size_bytes

        self._stats_gen = getattr(self, "_stats_gen", 0) + 1
        gen = self._stats_gen
        path = self._meetings_path_var.get()
        self._meetings_stats_label.configure(text="Подсчёт…")

        def worker() -> None:
            n = count_meetings(path)
            size = _folder_size_bytes(path)

            def apply() -> None:
                if gen != self._stats_gen:
                    return  # stale — user already switched to another folder
                word = plural_ru(n, "встреча", "встречи", "встреч")
                self._meetings_stats_label.configure(
                    text=f"В этой папке: {n} {word} • {_fmt_size(size)}",
                )

            try:
                self.after(0, apply)
            except tk.TclError:
                pass  # dialog destroyed while the walk was running

        threading.Thread(target=worker, daemon=True).start()
```

Note: no broad `except` — `count_meetings` returns 0 for missing paths and
`_folder_size_bytes` swallows per-file `OSError` internally. The #140
ratchet baseline (`settings.py: 3`) must stay unchanged.

- [ ] **Step 2: Run to verify green**

Run: `py -3 -m pytest tests/test_settings_async_stats.py tests/test_broad_except_ratchet.py -v`
Expected: all PASS

- [ ] **Step 3: Commit**

```bash
git add ui/dialogs/settings.py tests/test_settings_async_stats.py
git commit -m "fix(settings): compute folder stats off the Tk thread"
```

### Task 3: PR-2 gate + push

- [ ] **Step 1: Full gate** — `py -3 -m pytest` green; ruff clean.
- [ ] **Step 2: Push `fix/settings-async-stats`, open PR** (body: problem = Settings freeze on open with big recordings/; fix = daemon thread + generation counter + TclError guard; user smoke: open Settings with the real vault — instant open, stats fill in after a beat; switch folder mid-count). **Wait for user merge before PR-3.**

---

## PR-3 — dedup checkbox in Settings

Branch: `feat/settings-dedup-checkbox` (from main, after PR-2 merges).
PR title: `feat(settings): dedup on/off checkbox (Интеграции tab)`.

### Task 1: source-slice tests (red)

**Files:**
- Create: `tests/test_settings_dedup_section.py`

- [ ] **Step 1: Write the failing tests**

```python
"""Dedup on/off section in Settings (spec 2026-06-11, PR-3).

Source-text checks — no ui imports on Linux CI. The consumer gate
(extract_tasks reading config["dedup_enabled"]) is already locked by
tests/test_dialog_dedup_ui.py; these tests pin the Settings side.
"""
from pathlib import Path

BUILDER = Path("ui/dialogs/settings_builder.py").read_text(encoding="utf-8")
SETTINGS = Path("ui/dialogs/settings.py").read_text(encoding="utf-8")


def _section_block() -> str:
    start = BUILDER.index("def build_dedup_section")
    nxt = BUILDER.find("\ndef ", start + 1)
    return BUILDER[start:nxt if nxt != -1 else len(BUILDER)]


def test_builder_has_dedup_section():
    assert "def build_dedup_section" in BUILDER


def test_dedup_checkbox_bound_to_config_key_and_saves():
    block = _section_block()
    assert "CTkCheckBox" in block
    assert '"dedup_enabled"' in block
    assert "save_config" in block


def test_dedup_default_matches_consumer_gate():
    # Must mirror extract_tasks' config.get("dedup_enabled", True) — a
    # missing key means ON on both sides.
    assert 'get("dedup_enabled", True)' in _section_block()


def test_settings_wires_dedup_section_on_integrations_tab():
    assert "build_dedup_section" in SETTINGS
```

- [ ] **Step 2: Run to verify they fail**

Run: `py -3 -m pytest tests/test_settings_dedup_section.py -v`
Expected: 4 FAIL (`build_dedup_section` does not exist)

### Task 2: implement section + wiring

**Files:**
- Modify: `ui/dialogs/settings_builder.py` — new function after `build_trello_section` (ends line 432)
- Modify: `ui/dialogs/settings.py:159` — wire the section

- [ ] **Step 1: Add the section function**

Insert after `build_trello_section` in `settings_builder.py` (all names —
`ctk`, `FONT`, `TEXT_PRIMARY`, `BLUE`, `BLUE_DIM`, `BORDER`, `section_card`,
`save_config` — are already imported in this module; style copied from the
RNNoise checkbox in `build_audio_section`):

```python
def build_dedup_section(dialog, parent) -> None:
    """Dedup on/off — the only user-facing dedup knob (spec 2026-06-11).

    dedup_fuzzy_high / dedup_fuzzy_low stay config-only expert knobs
    (tasks.dedup.resolve_thresholds guards garbage). The consumer gate
    is the Extract dialog reading config.get("dedup_enabled", True).
    """
    section = section_card(dialog, parent, "Дубли задач", row=4)

    dialog._dedup_enabled_var = ctk.BooleanVar(
        value=bool(dialog._parent._config.get("dedup_enabled", True)),
    )

    def _on_toggled() -> None:
        dialog._parent._config["dedup_enabled"] = bool(
            dialog._dedup_enabled_var.get(),
        )
        save_config(dialog._parent._config)

    ctk.CTkCheckBox(
        section,
        text="Проверять дубли перед отправкой (комментарий вместо новой карточки)",
        variable=dialog._dedup_enabled_var,
        command=_on_toggled,
        font=ctk.CTkFont(family=FONT, size=13),
        text_color=TEXT_PRIMARY, fg_color=BLUE, hover_color=BLUE_DIM,
        border_color=BORDER, corner_radius=4,
        checkbox_height=20, checkbox_width=20,
    ).grid(row=0, column=0, columnspan=2, padx=4, pady=6, sticky="w")
```

(`row=4` — Интеграции tab currently holds OpenRouter=0, Linear=1, Glide=2,
Trello=3; verified 2026-06-11.)

- [ ] **Step 2: Wire it in settings.py**

After line 159 (`settings_builder.build_trello_section(self, scroll_integrations)`):

```python
        settings_builder.build_dedup_section(self, scroll_integrations)
```

- [ ] **Step 3: Run to verify green**

Run: `py -3 -m pytest tests/test_settings_dedup_section.py tests/test_widget_tree_split.py tests/test_bundle_ui_only.py -v`
Expected: all PASS (free function in builder — no `def _build_` in the class, no forbidden markers touched)

- [ ] **Step 4: Commit**

```bash
git add ui/dialogs/settings_builder.py ui/dialogs/settings.py tests/test_settings_dedup_section.py
git commit -m "feat(settings): dedup on/off checkbox (Интеграции tab)"
```

### Task 3: PR-3 gate + push

- [ ] **Step 1: Full gate** — `py -3 -m pytest` green; ruff clean.
- [ ] **Step 2: Push `feat/settings-dedup-checkbox`, open PR** (user smoke: Settings → Интеграции → toggle off → config.json shows `"dedup_enabled": false` → Extract run skips dedup badges; toggle back on). **Wait for user merge before PR-4.**

---

## PR-4 — honest cost forecast + forecast-vs-actual

Branch: `feat/extract-cost-forecast` (from main, after PR-3 merges).
PR title: `feat(extract): per-model cost forecast + forecast-vs-actual`.

### Task 1: `estimate_cost` in pricing.py (TDD)

**Files:**
- Modify: `tests/test_extract_pricing.py` (update 3 hint tests, add estimate_cost tests)
- Modify: `ui/dialogs/extract_tasks/pricing.py`

- [ ] **Step 1: Update + add tests (failing first)**

In `tests/test_extract_pricing.py`, extend the import (line 11):

```python
from ui.dialogs.extract_tasks.pricing import (
    estimate_cost,
    estimate_cost_hint,
    format_real_cost,
)
```

(Ensure `import pytest` is present at the top; add if missing.)

REPLACE the three `estimate_cost_hint` tests (the welcome test, the
`_1_3_fudge_factor` test — the fudge is being removed — and the
at-50-chars test) with:

```python
def test_estimate_cost_hint_below_50_chars_is_welcome():
    welcome = "Готов к работе. Извлеките из транскрипта или добавьте задачу вручную."
    assert estimate_cost_hint(0, "google/gemini-3.5-flash") == welcome
    assert estimate_cost_hint(49, "google/gemini-3.5-flash") == welcome


def test_estimate_cost_uses_selected_model_rates():
    # 4M chars → exactly 1M input tokens; gemini-3.5-flash = $1.50/$9.00.
    # Output ≈ 12% of input → 120k tokens. 1.50 + 0.12·9.00 = $2.58.
    cost = estimate_cost(4_000_000, "google/gemini-3.5-flash")
    assert cost == pytest.approx(1.50 + 0.12 * 9.00)


def test_estimate_cost_unknown_model_falls_back_to_flat_rate():
    # Custom OpenRouter slugs still get a ballpark: flat $3/1M both ways.
    cost = estimate_cost(4_000_000, "custom/who-dis")
    assert cost == pytest.approx(3.0 + 0.12 * 3.0)


def test_estimate_cost_hint_formats_model_rate():
    assert estimate_cost_hint(4_000_000, "google/gemini-3.5-flash") == (
        "Стоимость ≈ $2.58 (≈ 1,000,000 токенов)"
    )


def test_estimate_cost_hint_at_50_chars_shows_cost_not_welcome():
    assert estimate_cost_hint(50, "google/gemini-3.5-flash") == (
        "Стоимость ≈ $0.00 (≈ 12 токенов)"
    )
```

- [ ] **Step 2: Run to verify they fail**

Run: `py -3 -m pytest tests/test_extract_pricing.py -v`
Expected: ImportError on `estimate_cost` (and signature failures)

- [ ] **Step 3: Implement in pricing.py**

Replace the `_COST_PER_1M_INPUT_TOKENS_USD` comment + `estimate_cost_hint`
with (keep `_MODEL_PRICING_USD_PER_M` and `format_real_cost` as is):

```python
# Flat fallback rate per 1M tokens for models missing from the table —
# custom OpenRouter slugs still get a ballpark forecast.
_COST_PER_1M_INPUT_TOKENS_USD = 3.0

# Assumed completion-to-prompt ratio for the upfront forecast.
# Calibration: a typical 1-hour meeting ≈ 50k chars ≈ 12.5k input tokens
# yields ~1.5k tokens of task-JSON (10–20 tasks) → ≈ 0.12. Replaces the
# old flat ×1.3 input-rate fudge with an honest output term.
_EST_OUTPUT_RATIO = 0.12

# Below this transcript length the dialog is in the manual/dictation
# flow — no forecast is shown (and none is remembered for the
# forecast-vs-actual tail).
MIN_FORECAST_CHARS = 50


def estimate_cost(char_count: int, model: str) -> float:
    """Forecast extraction cost in USD using the SELECTED model's rates.

    Input tokens ≈ chars/4; output ≈ _EST_OUTPUT_RATIO × input. Unknown
    models fall back to the flat default rate for both directions.
    """
    approx_in = max(char_count // 4, 1)
    approx_out = int(approx_in * _EST_OUTPUT_RATIO)
    rates = _MODEL_PRICING_USD_PER_M.get(model)
    if rates is None:
        in_rate = out_rate = _COST_PER_1M_INPUT_TOKENS_USD
    else:
        in_rate, out_rate = rates
    return (approx_in * in_rate + approx_out * out_rate) / 1_000_000.0


def estimate_cost_hint(char_count: int, model: str) -> str:
    """Upfront status line: per-model forecast, or an adaptive welcome.

    Returns the welcome one-liner when there is effectively no transcript
    (< MIN_FORECAST_CHARS → manual / dictation flow), otherwise a
    «Стоимость ≈ $X.XX» forecast from estimate_cost.
    """
    if char_count < MIN_FORECAST_CHARS:
        # No transcript → manual-only flow; skip the cost line.
        return "Готов к работе. Извлеките из транскрипта или добавьте задачу вручную."
    approx_tokens = max(char_count // 4, 1)
    cost = estimate_cost(char_count, model)
    return f"Стоимость ≈ ${cost:.2f} (≈ {approx_tokens:,} токенов)"
```

Also update the module docstring's mention of the flat heuristic if it
contradicts (it describes helpers generally — adjust only if wrong).

- [ ] **Step 4: Run to verify green**

Run: `py -3 -m pytest tests/test_extract_pricing.py -v`
Expected: all PASS

- [ ] **Step 5: Commit**

```bash
git add tests/test_extract_pricing.py ui/dialogs/extract_tasks/pricing.py
git commit -m "feat(extract): per-model estimate_cost in pricing leaf"
```

### Task 2: dialog wiring (forecast memory + trace + success tail)

**Files:**
- Modify: `ui/dialogs/extract_tasks/__init__.py:56` (import), `:331-344` (`_update_cost_hint`), `:772-779` (success line)
- Modify: `ui/dialogs/extract_tasks/builder.py:206` (trace registration)
- Create: `tests/test_extract_cost_forecast.py`

- [ ] **Step 1: Write the failing source-slice tests**

Create `tests/test_extract_cost_forecast.py`:

```python
"""Forecast-vs-actual wiring in the Extract dialog (spec 2026-06-11, PR-4).

The pure math is unit-tested in test_extract_pricing.py; these slices pin
the dialog wiring (no ui imports — sounddevice on Linux CI).
"""
from pathlib import Path

EXTRACT = Path("ui/dialogs/extract_tasks/__init__.py").read_text(encoding="utf-8")
EXTRACT_BUILDER = Path(
    "ui/dialogs/extract_tasks/builder.py"
).read_text(encoding="utf-8")


def _method_block(src: str, name: str) -> str:
    start = src.index(f"def {name}")
    return src[start:src.index("\n    def ", start + 1)]


def test_cost_hint_uses_selected_model_and_remembers_forecast():
    block = _method_block(EXTRACT, "_update_cost_hint")
    assert "_model_var.get()" in block
    assert "_last_cost_forecast" in block


def test_model_change_reestimates_hint():
    # ComboBox edits (picked or typed) must re-run the forecast.
    assert "dialog._model_var.trace_add" in EXTRACT_BUILDER
    idx = EXTRACT_BUILDER.index("dialog._model_var.trace_add")
    assert "_update_cost_hint" in EXTRACT_BUILDER[idx:idx + 200]


def test_success_line_appends_forecast():
    block = _method_block(EXTRACT, "_on_extract_success")
    assert "прогноз $" in block
    assert "_last_cost_forecast" in block
```

- [ ] **Step 2: Run to verify they fail**

Run: `py -3 -m pytest tests/test_extract_cost_forecast.py -v`
Expected: 3 FAIL

- [ ] **Step 3: Wire the dialog**

`ui/dialogs/extract_tasks/__init__.py` line 56:

```python
from .pricing import MIN_FORECAST_CHARS, estimate_cost, estimate_cost_hint, format_real_cost
```

Replace `_update_cost_hint` (lines 331–344):

```python
    def _update_cost_hint(self) -> None:
        """Initial status line — per-model cost forecast or adaptive welcome.

        Phase 6.5 D — adaptive welcome (no transcript → no cost line).
        Spec 2026-06-11 — the forecast uses the SELECTED model's rates
        (re-run via the _model_var trace registered in builder.build_ui)
        and is remembered in _last_cost_forecast so _on_extract_success
        can show forecast-vs-actual.
        """
        chars = len(self._transcript or "")
        model = self._model_var.get().strip()
        self._last_cost_forecast = (
            estimate_cost(chars, model) if chars >= MIN_FORECAST_CHARS else None
        )
        self._status_label.configure(
            text=estimate_cost_hint(chars, model),
            text_color=TEXT_SECONDARY,
        )
```

In `_on_extract_success`, replace lines 772–779 with:

```python
        cost_str = self._format_real_cost(usage, used_model)

        parts = [f"✓ Извлечено {n} задач"]
        if corr:
            parts.append(f"({corr} полей скорректированы)")
        if cost_str:
            forecast = getattr(self, "_last_cost_forecast", None)
            if forecast is not None:
                # Spec 2026-06-11: close the trust loop — the actual next
                # to the upfront forecast the user saw before clicking.
                cost_str = f"{cost_str} (прогноз ${forecast:.4f})"
            parts.append(f"·  {cost_str}")
        self._status_label.configure(text="  ".join(parts), text_color=GREEN)
```

`ui/dialogs/extract_tasks/builder.py` — directly after line 206
(`dialog._update_cost_hint()`):

```python
    # Re-estimate when the model changes (picked or typed) — rates differ
    # per model (spec 2026-06-11). Registered AFTER the initial hint call
    # so the trace can never fire before _status_label exists. String
    # math only — cheap enough to run per keystroke in the ComboBox.
    dialog._model_var.trace_add("write", lambda *_: dialog._update_cost_hint())
```

Note: `dialog._model_var` (not `self._model_var`) — the
`test_bundle_ui_only.py` Whisper-ghost guard forbids `self._model_var`
in this file.

- [ ] **Step 4: Run to verify green**

Run: `py -3 -m pytest tests/test_extract_cost_forecast.py tests/test_extract_pricing.py tests/test_bundle_ui_only.py tests/test_widget_tree_split.py -v`
Expected: all PASS

- [ ] **Step 5: Commit**

```bash
git add ui/dialogs/extract_tasks/__init__.py ui/dialogs/extract_tasks/builder.py tests/test_extract_cost_forecast.py
git commit -m "feat(extract): per-model cost forecast + forecast-vs-actual"
```

### Task 3: PR-4 gate + push

- [ ] **Step 1: Full gate** — `py -3 -m pytest` green; ruff clean.
- [ ] **Step 2: Push `feat/extract-cost-forecast`, open PR** (user smoke: open Extract with a real transcript — hint shows gemini-rate forecast; type another model slug — hint re-estimates; run Извлечь — success line shows «… · X↑ + Y↓ т. · $A (прогноз $B)» with A and B in the same ballpark). Closes Variant 4.

---

## Self-review notes (already applied)

- Spec coverage: PR-1 ↔ spec §PR-1 (all 8 string sites + helper + 3 test
  changes), PR-2 ↔ §PR-2 (thread/generation/TclError), PR-3 ↔ §PR-3
  (checkbox/immediate-save/row=4), PR-4 ↔ §PR-4 (estimate_cost/
  MIN_FORECAST_CHARS export resolves the <50 duplication, trace, tail).
- Type consistency: `plural_ru(n, one, few, many) -> str` used
  identically in PR-1 Tasks 3–4 and PR-2; `estimate_cost(char_count,
  model) -> float` matches all call sites; `_last_cost_forecast:
  float | None` read via getattr default in success handler.
- Known-stale line numbers: PR-2..4 line refs describe post-PR-1 files
  within ±5 lines; executors must locate by symbol name, not raw number.
